void titletext(float size) {
  textSize(size);
  fill(black);
}

void normaltext(float size) {
  textSize(size);
  fill(black);
}

void bold(float size, color c1) {
  strokeWeight(size);
  stroke(black);
  fill(c1);
}
